# PRD — buddy：会成长的个人助手（MVP · 第 1 期）

> 项目：buddy（会成长的个人助手）｜ 版本：v1.0（MVP）｜ 日期：2026-09-19
> 上游依据：`research.md`（6 个竞品调研 + 本期不做清单）｜ 下游产物：Day 5 技术设计 → Day 7 可运行的 MVP
> 本次范围：只做「个人数据库」+「手机端联动」两项能力（其余见第 6 章）

## 1. 背景与目标

**要解决的问题**（节选自 research.md 第 1 章）：
1. AI 助手每次会话都“失忆”，纠正过的规则下次依然会犯；
2. 资料散落各处，想让 AI 基于“我的资料”回答却做不到，主流产品的记忆不可查看、不可编辑、不可迁移；
3. 自己整理的笔记不清晰、存放混乱，缺的不是记录工具而是可检索的组织方式；
4. 多端同步 + 免费使用这个组合没人做好（桌面优先、手机将就）；
5. 从手机发起任务、确认操作不方便，多数方案依赖桌面窗口常开；
6. 只有记录型产品，没有“经我确认后替我行动”的行动型助手。

**本期目标**：让 buddy 先具备两个可用的地基能力——
- **个人数据库**：把资料以 Markdown 存入固定结构，可检索、能定位原文、随资料变化自动更新索引；
- **手机端联动**：不依赖桌面端常开窗口，用手机浏览器即可发起任务、查看进度、确认操作。

**成功指标（本期结束时）**：
- 我能在一台只带手机的场合（无电脑）完成“记一条资料 → 查回来 → 发起一个任务”的完整闭环；
- 资料全部是纯文本文件，随时可整体导出/迁移，且个人资料不出现在公开仓库中；
- **连续 3 天、每天至少完成一次上述闭环**（说明这不是一次性的演示，而是日常可用的能力）。

## 2. 用户与用户故事

**目标用户**：我自己（学生，单人使用，不使用多人协作功能）。

**典型场景**：
- 场景 A：上课/路上突然想到一件事，掏出手机记下来，晚上在电脑上能检索到；
- 场景 B：想在资料里找“上周记录的某个结论”，用关键词搜出来并看到原文出处；
- 场景 C：手机上发起一个任务，之后在手机或电脑上都能看到它的进度状态。

**用户故事**：
- US1（对应 F1）：作为使用者，我希望**在手机上快速新建一条资料**，以便随手记下的东西不会丢。
- US2（对应 F2）：作为使用者，我希望**用关键词检索我的资料并点开原文**，以便确认结论的出处。
- US3（对应 F3）：作为使用者，我希望**新增或修改资料后索引自动更新**，以便不用手动维护列表。
- US4（对应 F4）：作为使用者，我希望**在手机上发起任务**，以便离开电脑也能让 buddy 干活。
- US5（对应 F5）：作为使用者，我希望**随时看到任务进度**，以便知道 buddy 做完了没有。
- US6（对应 F6）：作为使用者，我希望**高风险操作在手机上由我确认后才执行**，以便它不会擅自替我做事。
- US7（对应 F7）：作为使用者，我希望**数据都在我自己的仓库里并能随时导出**，以便不被任何平台绑定。

## 3. 功能清单与优先级

| 编号 | 功能 | 所属能力 | 优先级 | 说明 |
|---|---|---|---|---|
| **F1** | 资料录入 | 个人数据库 | **P0** | 手机/电脑新建 Markdown 条目，按固定目录与命名规范存放 |
| **F2** | 资料列表与检索 | 个人数据库 | **P0** | 按关键词/日期检索，结果可点开定位到原文 |
| **F3** | 索引自动更新 | 个人数据库 | **P0** | 打开列表页时索引自动重建，无需手动维护 |
| **F4** | 手机端发起任务 | 手机端联动 | **P0** | 手机浏览器打开网页即可提交任务，不依赖桌面窗口 |
| **F5** | 手机端查看进度 | 手机端联动 | P1 | 任务状态（待办/进行中/完成）可见 |
| **F6** | 手机端确认操作 | 手机端联动 | P1 | 删除、对外发送等高风险动作需在手机上确认才执行 |
| **F7** | 数据自托管与可迁移 | 两个能力共同 | **P0** | 资料为纯文本、存于自有仓库，可整体导出 |
| **F9** | 知识库问答（RAG） | 个人数据库 | P1 | 对资料库自然语言提问：先检索相关段落再让模型生成，回答附来源文件与段落号；支持流式输出（2026-09-25 追加） |

**P0 = 本期必须完成；P1 = 本期尽量完成，实在紧张可延后一天。**

## 4. 验收标准

> 判定原则：每条都必须能由人“点一下、看到结果”来验证；含时间/数量要求的写具体数字。
> 时间类标准默认在「常用网络、资料总量 ≤100 条」条件下验证。

### F1 资料录入
**资料存在哪里（说清楚）**：个人资料放进**自己的私有数据仓库**（自建服务器上的 Gitea），**不放进公开的 GitHub 仓库**——公开仓库里只有代码和项目文档，别人打开看不到你的任何资料。
- [ ] 手机浏览器打开页面，填写标题与正文，点保存后**能在资料目录里找到这个文件**（路径形如 `memory/学习/2026-09-19-标题.md`）；
- [ ] 打开公开仓库（GitHub）**看不到任何个人资料文件**；
- [ ] 保存失败时页面上给出**明确提示**（不能没反应）——验证方式：断网后再点保存，应看到明确提示；
- [ ] 整个过程**不需要打开电脑**；
- [ ] 电脑浏览器打开同一地址也能录入（与手机端一致，验收以手机为准）。

### F2 资料列表与检索
- [ ] 输入关键词后**在 2 秒内**列出匹配条目；
- [ ] 每条结果**能点开并定位到原文**（看到所属文件与内容）；
- [ ] 支持按日期排序；无结果时显示“没有找到”而不是空白页。

### F3 索引自动更新
**本期做法（说清楚）**：网页版没有常驻后台，所以索引采用**打开时自动重建**——你不用管它，打开列表页它就自己刷新好了。
- [ ] 新增或修改资料后，**下次打开列表页时索引自动重建**（页面上能看到"正在更新"的提示，耗时不超过 3 秒）；
- [ ] 重建完成后，新资料**立刻能被搜到**；
- [ ] 使用者**不需要执行任何命令、不需要手动维护列表**；
- [ ] 索引文件**不出现在公开仓库中**（属于服务端临时数据，不入库）。

### F4 手机端发起任务
**访问方式（本期已定）**：网页部署在**公网**，用 **HTTPS** 地址访问——手机浏览器直接打开，**不需要安装任何客户端**；页面**默认公开、免登录**使用（隐私模块默认关闭，本期不做登录鉴权）。
- [ ] 手机**关闭 WiFi 使用移动网络**时，能打开页面并读写资料；
- [ ] 提交一个任务（例如：「让 buddy 记一条资料：今天学了 X」）后，电脑端 `git pull` 或服务器端能看到该任务记录；
- [ ] 页面加载在常用网络下**不超过 5 秒**。

### F5 手机端查看进度
- [ ] 任务列表可显示状态（待办 / 进行中 / 完成）；
- [ ] 状态变更后刷新页面即可看到最新状态。

### F6 手机端确认操作
- [ ] 删除类、对外发送类操作会先展示**确认页**（用大白话写明"接下来会发生什么"）；
- [ ] **没有点确认就绝不执行**（可通过确认日志或结果验证）；
- [ ] 每次确认都留痕：记录**确认时间**与**确认内容**（改了什么、将要发生什么），页面上可回看（单人使用，不需要记录"谁"）。

### F7 数据自托管与可迁移
- [ ] 个人资料目录**不在公开仓库的跟踪范围内**（`git status` 不会列出个人资料文件）；
- [ ] 一条命令即可导出全部资料（例如把目录打包或 `git clone`）；
- [ ] 在**另一台设备**拉取仓库后可继续使用，原有资料完整。

### F9 知识库问答（RAG）
**做法（说清楚）**：资料切块向量化存入 Chroma（自托管于服务器）；提问时先检索最相关的若干段落（默认 3 段），连同问题一起交给大模型生成回答，**回答末尾标注来源文件名与段落号**；支持流式输出（逐字显示）。
- [ ] 问一个资料里有的问题，回答**来自资料内容**且能看到来源文件（点击来源可跳到该资料原文）；
- [ ] 问一个资料里没有的问题，明确被告知「没有找到相关内容」，**不编造**；
- [ ] 流式接口能看到**逐字输出**（不是等很久一次性弹出）；
- [ ] 新增一条资料并触发索引后，再提问能命中新资料（增量索引，无需重建全库）；
- [ ] 后端缺配置（没配 key）时接口返回**明确的中文错误**，页面显示提示而不是无反应；
- [ ] 手机浏览器打开问答页可正常使用（输入框 ≥16px、按钮 ≥44px 触控目标）。

### 全局验收
- [ ] 本文档的每条验收标准都能**照着复现**（不需额外解释）；
- [ ] 不出现“密钥、密码、连接串”进入代码或提交记录的情况。

## 5. 数据与同步策略（本期约定，Day 5 细化）

| 内容 | 存放位置 | 理由 |
|---|---|---|
| 代码与项目文档 | GitHub 公开仓库：`bird-z/vbcd`（origin）+ `XingHo-VibeCoding/vbcd`（camp，组织仓） | 打卡、验收、可从链接直接查看 |
| 个人资料数据（即"资料目录"） | **自建 Gitea 私有仓库**（阿里云 ECS：2 vCPU / 4GB，已有域名） | 个人数据不外泄，且完全自有、可迁移 |
| 索引与本地数据库文件 | 服务端临时数据（不入库，可再生） | 属于可重新生成的派生数据 |

**资料目录约定**（后文统一叫"资料目录"，即 `memory/`）：

```
memory/
├── learning/   # 学习留痕（B 站、网页）
├── life/       # 生活记录
├── work/       # 事务与任务相关
└── rules.md    # 助手长期规则（用户纠正，新会话必读）
```

命名规范：`memory/<分类>/YYYY-MM-DD-标题.md`（日期 + 主题，人能直接看懂）。

- **写入通道**：网页服务端**代表使用者**，把资料提交进私有数据仓库（使用者自己不必直连 Gitea，手机端只需打开网页）。
- **访问方式（主通道）**：网页部署在公网，**HTTPS 访问**（隐私模块默认关闭，本期不加登录鉴权；手机浏览器直接打开，不需装客户端）。采用这条的前提是：域名可正常解析（境内服务器需先完成备案），或服务器在境外。
- **备用通道**：如果备案尚未完成、又急着用，可先用 **Tailscale 组网**过渡（不暴露公网、免备案），等备案通过再切回公网 HTTPS。
- **安全底线（公网暴露必须做到）**：只开放 HTTPS 端口；Gitea 账号使用强密码 + 两步验证；关闭公开注册；个人资料默认公开（隐私模块默认关闭；需门禁时再设 `AUTH_ENABLED=1`）。
- **同步方式**：Git（多远程：一份内容可同时推 GitHub 公开仓与自建私有仓）。
- **备份要求**：自建仓库需有第二份备份（本地镜像 + 云快照），避免服务器单点故障丢数据。

## 6. 不做什么（本期边界）

沿用 `research.md` 第 4 章，本期明确不做：

| 不做项 | 原因 / 何时再做 |
|---|---|
| smail 邮箱关联 | 第 2 期（依赖个人数据库先建好） |
| ehall 事务（查询/填表/提交） | 第 2–3 期（涉及外部提交，风险最高，须先有确认机制） |
| 深度能力组合（跨工具完整流程） | 第 3 期（五项能力各自可用后再组合） |
| 自动后台记录 / 被动捕获 | **永远不做**（违背“确认优先”铁律） |
| 多用户 / 团队协作 | 不做（定位是个人助手） |
| 原生 App（iOS/Android） | 不做（本期用网页版覆盖手机端） |
| 语音输入 / 硬件 | 不做 |
| RAG 进阶优化：混合检索（BM25+RRF）、Rerank 重排序、多轮对话记忆 | 本期只做核心问答（F9）；资料量小，等检索质量不足时再做 |
| 技术选型与架构细节 | 不在本 PRD（Day 5 的技术设计文档负责） |

## 7. 后续候选（第 2 期优先）

### F8 学习留痕（先把想法记下来，不在本期实现）
- **背景**：日常在 B 站与网页学习，但很少沉淀、很少留痕（真实痛点，来自使用者自述）。
- **设想流程**：输入链接 → AI 生成结构化条目（来源 / 一句话收获 / 关键点 / 待验证 / 我的疑问）→ 使用者确认 → 归档到 `memory/learning/` → 每周自动汇总成回顾。
- **难度与排期（评估于 2026-09-19）**：

| 部件 | 难度 | 排期 |
|---|---|---|
| 输入链接 + 生成条目 + 确认归档 + 周汇总 | 低（≤1 天） | **第 2 周（Day 12 自制 Skill）**：做成一个可复用 Skill + 复用 F1 的录入界面 |
| 网页正文自动抓取与摘要 | 中 | 第 3 周（服务端实现） |
| B 站视频信息/字幕自动获取 | 中高（接口有签名与登录态要求，易失效） | 第 3 周，且做降级设计（抓不到字幕时用标题+简介+手动补要点） |

- **额外价值**：可作为 NJU 实验「能力组合」的演示流程（链接 → 摘要 → 追问 → 手机确认 → 归档 → 周汇总）。

## 8. 风险与待确认项

1. **服务器地域与备案**：ECS 在境内且域名未备案时，需先走 Tailscale 过渡（实施前确认）；
2. **训练营第 3 周的验收口径**：手册按 CloudBase 编排，若改用自建服务器部署，需先与队长确认是否认可；
3. **手机端编辑体验**：Markdown 在手机浏览器上编辑不如原生 App，需在第 2 周（前端阶段）重点打磨；
4. **索引方案复杂度**：列表检索仍用最轻量的实现（文件列表 + 关键词匹配）；向量检索仅在 2026-09-25 追加的 F9 问答链路中引入（Chroma 自托管），列表页不依赖它，向量库故障不影响 F1–F3；
5. **依赖 Tailscale 第三方控制面**（仅备用通道会用到）：身份与协调由第三方提供；若不可接受，改用自建 Headscale 或直接公网 HTTPS；
6. **网页/B 站抓取接口易失效**（第 2–3 期的 F8 会用到）：届时必须做降级设计（抓不到就退回手动输入）。

## 9. 变更记录

| 日期 | 版本 | 改了什么 |
|---|---|---|
| 2026-09-19 | v1.0 | 初版：F1–F7 功能与验收标准、数据与同步策略、本期边界、后续候选 F8 |
| 2026-09-19 | v1.1 | 自检后修订：明确资料存放与访问方式（公网 HTTPS + 登录）；索引改为打开时自动重建；补充资料目录约定、验证条件、任务示例、变更记录 |
| 2026-09-23 | v1.2 | 登录调整为「隐私模块」默认关闭（站点公开免登录，`AUTH_ENABLED=1` 可启用）；同步更新访问方式与安全底线 |
| 2026-09-25 | v1.3 | 追加 F9 知识库问答（RAG）：功能条目、验收标准 6 条；不做清单补 RAG 进阶优化；风险 4 修订为「向量检索仅用于问答链路」 |
