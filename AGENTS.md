# AGENTS.md — buddy（会成长的个人助手）工作规则

> 本文件是 AI 助手在本项目工作时必须遵守的规则书。每次会话开始先读本文件。

## 1. 项目定位

- `buddy` 是一个持续为我工作的个人助手实验项目（NJU 实验：实现会成长的个人助手）。
- 五项能力：个人数据库 / smail 邮箱关联 / ehall 事务 / 手机端联动 / 能力组合。
- 本项目位于 `vbcd` 仓库的 `buddy` 分支（与训练营主线 `main` 分开）。

## 2. 铁律（任何时候不得违反）

1. **外部操作必须确认**：发送邮件、提交表单、退课、撤销申请等一切对外动作，先展示关键字段和后果，等用户明确同意后才能执行。
2. **不重复处理**：同一封邮件不重复处理或发送；重复性任务执行前先查重。
3. **密钥与隐私不入库**：`.env`、密码、API 密钥、数据库连接串永不进代码和提交；本地数据库文件、索引文件不入 Git。
4. **保守优先**：宁可多做一步确认，不猜、不代办用户的决定。

## 3. 记忆与纠错

- 用户指出的每一条纠正（如"不要把报名截止时间当成活动开始时间"）都要写入 `memory/rules.md`，成为**长期生效的规则**，新会话必须先读取并遵守。
- 成功跑通的流程整理成 `skills/` 下的可读脚本或说明，供复用和人工修改。
- 用户可随时直接编辑 `memory/` 与 `skills/`，助手下一次会话以文件内容为准。

## 4. 交互约定

- 用户未确认的产物（草稿、表单）放在 `drafts/`，等待核对。
- 每次执行跨工具任务时，先说明要动哪些数据、产出什么，再动手。
- 术语和命令对用户保持"一两句话能懂"的解释标准。

## 5. 目录约定

- `memory/`：个人资料、长期记忆、纠错规则（`memory/rules.md`）
- `skills/`：可复用流程
- `drafts/`：待确认草稿
- 索引与数据库文件（`*.db`、`index_data/` 等）只存本地，已列入 `.gitignore`
