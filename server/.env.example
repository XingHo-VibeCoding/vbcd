# 复制为 .env 并填入真实值（.env 不入库，已在 .gitignore）
# 本步骤（1a 骨架）只用到 PORT / NODE_ENV；下面带注释的字段留待后续子步骤（1b 登录 / 1c 存储）再填
PORT=3000
NODE_ENV=development
LOG_LEVEL=info

# 1b 登录用（到时再填）：
# SESSION_SECRET=
# PASSWORD_HASH=

# 1c 存储用（到时再填）：
# DATA_DIR=
